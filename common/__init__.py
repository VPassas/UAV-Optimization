"""Shared, co-owned modules for the UAV networking project (Student A + Student B).

Submodules
----------
channel      Full Al-Hourani air-to-ground channel model (re-exported from the
             original simulator so there is a single source of truth).
data_loader  EUBUCCO / OSM town loader -> buildings + roads + heights.
"""
